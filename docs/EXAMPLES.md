# Полнота примеров: что есть и чего не хватает

Сверка 18 разделов курса из [README](../README.md) с фактическим содержимым модулей.
Проверено на 04.08.2026.

## 1. По разделам курса

| # | Раздел | Что реализовано | Чего не хватает |
|---|---|---|---|
| 1 | Асимптотика | `vol1/bigo/Main` — демонстрация без тестов; `commons/Counter` считает шаги | Стенд, который меряет фактическое число операций и показывает разницу классов O(1)/O(log n)/O(n)/O(n log n)/O(n²) на растущих входах |
| 2 | Линейные структуры | `Array` (массив и список), `Stack`, `Queue`, скобочная последовательность, обратная польская запись | Двусвязный список, дек, список с пропусками. Кольцевой буфер есть, но лежит в `vol_/buffer` и к разделу не привязан |
| 3 | Рекурсия | Факториал, Фибоначчи, судоку с возвратом, разбор выражений, виртуальная машина, ребусы | Ханойские башни, восемь ферзей, мемоизация как отдельный приём. Трамплин лежит в `vol_/trampoline` и к разделу не привязан |
| 4 | Комбинаторика | Перестановки, размещения, сочетания — с ленивым перебором | Разбиения числа, размещения с повторениями, код Грея, треугольник Паскаля. Джонсон — Троттер лежит в `vol_/jt` |
| 5 | Сортировка | Слиянием, подсчётом, быстрая, пирамидальная, поразрядная | Квадратичные сортировки (пузырьком, вставками, выбором) — без них не с чем сравнивать; Шелла; проверка устойчивости; внешняя сортировка |
| 6 | Поиск | Двоичный по индексу, по натуральным, по вещественным | Интерполяционный, троичный, экспоненциальный поиск |
| 7 | Хеширование | Полиномиальный хеш и его кэш, поиск подстроки хешем, хеш-таблица на цепочках | Открытая адресация, фильтр Блума, консистентное хеширование. Перехеширование помечено как пазл и не сделано |
| 8 | Графы | Обход в глубину и в ширину, поиск циклов, Дейкстра, муравьиный алгоритм | Беллман — Форд (отрицательные веса), Флойд — Уоршелл (все пары), A\*, компоненты сильной связности, максимальный поток, проверка двудольности |
| 9 | Куча | Двоичная куча минимума | Куча с уменьшением ключа — без неё Дейкстра работает на переборе; d-арная куча; сравнение с биномиальной |
| 10 | Непересекающиеся множества | Сжатие путей, окрашивание | Полно |
| 11 | Топологическая сортировка | Кан, обход в глубину | Полно |
| 12 | Остовные деревья | Крускал, Прим | Полно |
| 13 | Деревья | BST, AVL, красно-чёрное, Хаффман, центроид, диаметр, наименьший общий предок, эйлеров обход | Дерево отрезков, дерево Фенвика, бор для строк, B-дерево, декартово дерево |
| 14 | Динамическое программирование | Левенштейн, наибольшая общая подпоследовательность, Фибоначчи, рюкзак, шаблоны с подстановками | Наибольшая возрастающая подпоследовательность, размен монет, разрезание стержня, ДП по подмножествам |
| 15 | Строки | Кнут — Моррис — Пратт, Рабин — Карп | Ахо — Корасик, Z-функция, Бойер — Мур, суффиксный массив |
| 16 | Конечные автоматы | Разбор регулярных выражений в дерево и автомат, сопоставление, одноразовые пароли | Детерминизация НКА в ДКА и минимизация ДКА — классика раздела, которой нет |
| 17 | Лабиринт | Генерация и поиск пути, отрисовка в GIF | Полно |
| 18 | Клеточный автомат | Жизнь, матрица байтов | Полно |

Разделы 10 — 12, 17 и 18 закрыты. Самые заметные дыры — разделы 1, 5, 8, 15 и 16.

## 2. Примеры не на своих местах

Модуль `vol_` заявлен как песочница вне программы, но четыре примера из него прямо относятся
к разделам курса и просто не связаны с ними ссылками:

| Пример | Куда относится |
|---|---|
| `vol_/buffer/CircularBuffer` | Раздел 2, линейные структуры |
| `vol_/trampoline/Trampoline` | Раздел 3, рекурсия без роста стека |
| `vol_/jt/JohnsonTrotter` | Раздел 4, перестановки |
| `vol_/prime/SieveOfEratosthenes` | Раздел 1, оценка сложности решета |

Дешевле всего закрыть это строчками в таблице разделов `README`, не двигая код.

## 3. Полнота покрытия тестами

| Модуль | Классов без единого упоминания в тестах |
|---|---|
| `commons` | `Counter` — единственный класс модуля |
| `vol3` | `BinaryNaturalSearch`, `BinaryRealSearch` — два поиска из трёх |
| `vol5` | `DijkstraShortestPath`, `PathTree`, `Weight` |
| `vol6` | 15 из 26: `Centroid`, `Diameter`, `LowestCommonAncestorPath`, `MergePath`, все посетители |
| `vol7` | `Fibonacci`, `LongestCommonSubsequence`, `Distance` |
| `vol8` | `Machine`, `OTPMachine`, `Duplicate`, `Statistics`, `TextBuffer` |
| `vol_` | 26 из 57: кэши, коды, лабиринтные генераторы, `GoL`, `CircularBuffer`, `Trampoline` |
| `heroes/web` | 38 из 40; появились первые тесты на комнаты боя |

Тяжелее всего `vol6` и `vol5`: там без тестов остались именно те алгоритмы, ради которых
раздел и существует — Дейкстра, центроид, наименьший общий предок.

## 4. Что добавить и в каком порядке

Порядок — по отношению пользы к затратам: сначала то, что закрывает раздел целиком.

### Первая очередь — закрывают дыры разделов

1. **Квадратичные сортировки** (`vol3`): пузырьком, вставками, выбором. Без них раздел 5 не
   показывает главного — разницы между O(n²) и O(n log n) на одних и тех же данных. Каждая
   реализация — десяток строк, зато `Counter` сразу даёт цифры для сравнения.
2. **Стенд сложности** (`vol1/bigo`): прогон готовых алгоритмов на растущих входах с выводом
   таблицы «n → шагов». Раздел 1 сейчас единственный без проверяемого кода.
3. **Детерминизация и минимизация автомата** (`vol8`): НКА → ДКА подмножествами и алгоритм
   Хопкрофта. Разбор регулярных выражений уже есть, автомат строится — не хватает ровно того
   шага, ради которого раздел 16 и читают.
4. **Беллман — Форд и Флойд — Уоршелл** (`vol5`): отрицательные веса и все пары кратчайших.
   Дейкстра без них выглядит единственно возможным решением, а это не так.
5. **Куча с уменьшением ключа** (`vol6/tree`): свяжет разделы 8 и 9 — Дейкстра на такой куче
   даёт O(m log n) вместо нынешнего перебора.

### Вторая очередь — расширяют раздел вширь

6. **Дерево отрезков и дерево Фенвика** (`vol6`): запросы на отрезке, которых в разделе 13
   нет совсем, а на практике они встречаются чаще AVL.
7. **Ахо — Корасик** (`vol13`): поиск многих образцов сразу; ложится на бор и продолжает
   Кнута — Морриса — Пратта.
8. **Наибольшая возрастающая подпоследовательность и размен монет** (`vol7`): две
   канонические задачи ДП, каждая с двумя решениями — переборным и табличным.
9. **Открытая адресация** (`vol4`): вторая стратегия разрешения коллизий рядом с цепочками,
   плюс честное перехеширование вместо нынешнего пазла.
10. **Ханойские башни и восемь ферзей** (`vol2`): классика рекурсии, на которой видно
    ветвление и отсечение.

### Третья очередь — тесты на то, что уже написано

11. Тесты на `DijkstraShortestPath`, `Centroid`, `Diameter`, `LowestCommonAncestorPath` —
    алгоритмы разделов 8 и 13 сейчас не проверяются вообще.
12. Тесты на `BinaryNaturalSearch` и `BinaryRealSearch` — два поиска из трёх без проверки.
13. Тесты на `Counter`: на нём держатся все замеры сложности, а сам он не проверен.

### Что добавлять не стоит

Суффиксный массив, максимальный поток, B-дерево и декартово дерево в программе курса не
заявлены. Они уместны как задание со звёздочкой, но не как обязательный пример: раздел от
их отсутствия не рвётся.
