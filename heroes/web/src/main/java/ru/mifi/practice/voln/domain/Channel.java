package ru.mifi.practice.voln.domain;

/**
 * Канал событий одной стороны комнаты.
 *
 * <p>Комната отправляет строки, не зная, чем они доставляются. Благодаря этому подбор соперника
 * и передача ходов проверяются обычным тестом, без поднятия веб-слоя.
 */
public interface Channel {

    void send(String data);
}
